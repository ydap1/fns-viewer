# The repository is authored on macOS but launch.bat is run by cmd.exe, which
# wants CRLF — LF-only batch files can break on goto labels and () blocks.
# Git stores LF and hands out the right endings per file type on checkout.
* text=auto eol=lf
*.bat text eol=crlf
*.sh text eol=lf
